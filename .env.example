# Copy to .env and fill in. Personal token (simplest, single user):
MONEYBIRD_ACCESS_TOKEN=
# Leave blank if the token sees only one administration.
MONEYBIRD_ADMINISTRATION_ID=

MCP_HOST=127.0.0.1
MCP_PORT=8000
# Shared secret for the MCP endpoint; required for non-loopback MCP_HOST.
MCP_AUTH_TOKEN=
# Where server state lives (approvals DB, audit logs, sync caches, OAuth tokens).
# Defaults to the working directory; set an absolute path for real deployments.
MONEYBIRD_MCP_DATA_DIR=
# "stdio" (default of the `moneybird-mcp` console script; for local MCP clients),
# "http" (streamable HTTP, endpoint /mcp) or "sse" (legacy, endpoint /sse — the
# default of `python moneybird_mcp_server.py`).
MCP_TRANSPORT=sse

# OAuth alternative to a personal token: register an application at
# https://moneybird.com/user/applications/new, fill these in, then run
# python scripts/oauth_login.py once.
MONEYBIRD_OAUTH_CLIENT_ID=
MONEYBIRD_OAUTH_CLIENT_SECRET=
